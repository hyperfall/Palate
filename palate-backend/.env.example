# Owned recipe store (local now, cloud later).
BACKEND_DATABASE_URL=postgresql://postgres@127.0.0.1:5432/palate_backend

# Unsplash — quality photography (https://unsplash.com/developers → New App).
# Demo tier: 50 req/hour (1,000/hour after production approval). Leave empty to skip image attachment.
UNSPLASH_ACCESS_KEY=

# OpenAI — the Generative Chef rewrite pass (rewrite.py).
OPENAI_API_KEY=
